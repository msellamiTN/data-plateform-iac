name: finops
version: '1.0.0'
config-version: 2

profile: finops

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  finops:
    +materialized: view
    staging:
      +schema: staging
      +tags: ["staging", "account_usage"]
    marts:
      +schema: marts
      +tags: ["marts", "finops"]
      +materialized: table

vars:
  finops:
    start_date: "2025-01-01"
    monitoring_database: "SNOWFLAKE"
    monitoring_schema: "ACCOUNT_USAGE"
