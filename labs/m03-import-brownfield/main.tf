# ==============================================================================
# Lab M03 — Import Brownfield & Drift Detection
# ==============================================================================
# Objectif : Importer un objet existant (créé manuellement dans Snowsight)
# sans aucune destruction, détecter la dérive et réaligner le code.
# ==============================================================================

# TODO : Déclarer le bloc import ou la ressource pour l'objet existant
# import {
#   to = snowflake_database.brownfield
#   id = "${var.learner_prefix}_M03_LEGACY_${var.environment}"
# }
#
# resource "snowflake_database" "brownfield" {
#   name                        = "${var.learner_prefix}_M03_LEGACY_${var.environment}"
#   comment                     = "Imported brownfield database"
#   data_retention_time_in_days = 1
# }
