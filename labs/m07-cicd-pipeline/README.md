# Code de départ — M7 : Pipeline CI/CD

## Ce qu'il faut copier

Aucun code Terraform à copier. Les participants travaillent avec :
- `azure-pipelines.yml` (ADO principal)
- `.github/workflows/terraform.yml` (GH Actions référence)

## État de départ

Les participants configurent :
- Les service connections ADO
- Les groupes de variables pour les secrets
- Les politiques de branche

## Validation

Créer une PR pour déclencher le pipeline. Vérifier que tous les stages réussissent.

